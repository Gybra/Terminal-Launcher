package com.gybra.terminallauncher.shell

/**
 * The drive letter the DOS prompt is written with. Decorative only: the launcher never reads a
 * filesystem, so no drive exists behind the letter.
 */
public enum class DosDrive {
    A,
    C,
    D,
}
